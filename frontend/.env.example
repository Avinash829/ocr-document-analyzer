# Copy this file to frontend/.env.local for local development or configure
# this value in the frontend hosting platform before running the production build.

# Public browser-facing URL of the deployed FastAPI backend. Do not add secrets here.
NEXT_PUBLIC_API_URL=http://localhost:8000
